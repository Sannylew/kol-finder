# docker-compose 数据库凭证模板（已预设可用值，cp 为 .env 即可）
#   cp .env.example .env
#
# 注意：密码只在【首次创建数据卷】时生效。之后要改需 docker compose down -v
# 删卷重新初始化（会清空数据）。
# 此密码必须与 backend/.env 的 DATABASE_URL 中的密码一致（已预设为同一个）。

POSTGRES_USER=kol
POSTGRES_PASSWORD=MxjcL5QCEd4f15FP8iniiSz0
POSTGRES_DB=kol_finder
